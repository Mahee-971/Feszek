<%- include('partials/header', { pageTitle: 'Reported — Budapest Rentals' }) %>

<div class="report-box">
  <h1>Thanks for the report</h1>
  <p>We'll take a look at "<%= title %>" and remove it if it breaks the rules.</p>
  <a class="btn btn-ghost" href="<%= backHref %>">Back to browse</a>
</div>

<%- include('partials/footer') %>
